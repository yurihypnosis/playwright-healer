{
  "name": "relocator-monorepo",
  "private": true,
  "packageManager": "pnpm@11.9.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "bench:similo": "pnpm --filter @relocator/benchmarks bench:similo",
    "bench:mutations": "pnpm --filter @relocator/benchmarks bench:mutations"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
